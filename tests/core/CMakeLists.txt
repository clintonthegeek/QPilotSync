# K.8b T13: tst_ibackendplugin_defaults deleted along with IBackendPlugin V1.
# This subdirectory currently has no tests but is kept so add_subdirectory(core)
# in tests/CMakeLists.txt stays valid (and so future core ABI tests can land
# here).
