#include "ipluginaction.h"

// This translation unit exists solely so that AUTOMOC generates the
// meta-object (signal implementations + vtable) for
// IPluginAction::ActionContext, which carries Q_OBJECT.
